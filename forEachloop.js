var donuts = ['cold furry','marble gold','blazing'];
donuts.forEach(function(donut){
     donut += ' hole'
     donut.toLocaleUpperCase;
     console.log(donut);

})
//the forEach method is use to exceute function for each element in the array
//it can also be use to loop over element in the array